# Bazzite / gamescope custom refresh rate list
# Tells gamescope-session-plus to pass --custom-refresh-rates so Steam UI
# and Moonlight can negotiate higher refresh rates at 4K.
# Modes must already be present in the virtual monitor EDID (they are).
CUSTOM_REFRESH_RATES=60,90,120,144
